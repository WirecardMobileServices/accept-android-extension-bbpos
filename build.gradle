apply plugin: 'maven'

group='com.github.mposSVK'
version='release'

artifacts {
  archives new File('accept-sdk-android-bbposextension-release.aar')
}

uploadArchives {
  repositories {
    mavenDeployer {
      repository(url: mavenLocal().url)
    }
  }
}
